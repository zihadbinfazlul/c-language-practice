#include<stdio.h>
int main(){
int number,number2;
printf("input the number:");
scanf("%d", &number);
printf("input the another number:");
scanf("%d", &number2);
printf("the multiply is:%d",number*number2);
    return 0;
}